#!/usr/bin/make -f

# The importable package is pcbstencil; the binary package and the console
# script are called stencicrity.
export PYBUILD_NAME=pcbstencil

%:
	dh $@ --with python3 --buildsystem=pybuild
